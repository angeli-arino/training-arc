{
  "name": "training-arc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "node --check src/main.js && node --check src/bodyScanLogic.js && node --check scripts/build-check.mjs",
    "test": "node --test",
    "build": "node scripts/build-check.mjs",
    "dev": "node scripts/dev-server.mjs"
  }
}
